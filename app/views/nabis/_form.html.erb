<%= form_with(model: nabi, local: true) do |form| %>
  <% if nabi.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nabi.errors.count, "error") %> prohibited this nabi from being saved:</h2>

      <ul>
        <% nabi.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_area :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
